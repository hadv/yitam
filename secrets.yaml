apiVersion: v1
kind: Secret
metadata:
  name: yitam-secrets
  namespace: yitam
type: Opaque
data:
  # Base64 encoded secrets from prod.env should be added here
  # Example: DATABASE_URL: base64encodedstring
  # You can encode using: echo -n "your-secret" | base64 